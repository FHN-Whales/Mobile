import { StyleSheet } from 'react-native';
const optionHeathCheck = StyleSheet.create({
    container:{
        flex:1,
        flexDirection:'column',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor:'#fff',
        gap:20,
    },
});
export default optionHeathCheck;
